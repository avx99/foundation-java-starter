plugins {
    id("foundation.api")
}

/*

NOTE: Appliquer le plugin 'foundation.api' est un raccourci pour la configuration suivante

plugins {
    id("foundation.java8")
    id("foundation.test.junit5")
    id("foundation.test.karate")
}

dependencies {
    api("dev.soffa.foundation:foundation-api:${property("f4j.version")}")
}
 */
